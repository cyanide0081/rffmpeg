#ifndef HEADER_CONVERT
#define HEADER_CONVERT

#include <data.h>
#include <parse.h>

int convertFiles(const char **files,
                 arguments *args,
                 processInfo *stats);

#endif  /* HEADER_CONVERT */
